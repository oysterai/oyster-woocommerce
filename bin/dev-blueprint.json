{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "landingPage": "/wp-admin/admin.php?page=oyster-woocommerce",
  "preferredVersions": {
    "php": "8.1",
    "wp": "latest"
  },
  "phpExtensionBundles": ["kitchen-sink"],
  "features": {
    "networking": true
  },
  "login": true,
  "steps": [
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "wordpress.org/plugins",
        "slug": "woocommerce"
      },
      "options": {
        "activate": true
      }
    },
    {
      "step": "setSiteOptions",
      "options": {
        "permalink_structure": "/%postname%/"
      }
    },
    {
      "step": "defineWpConfigConsts",
      "consts": {
        "OYSTER_WOO_API_BASE_URL": "https://api.oysterskin.com"
      }
    },
    {
      "step": "runPHP",
      "code": "<?php\nrequire_once '/wordpress/wp-load.php';\n\n// Skip the WooCommerce setup wizard redirect — this is a disposable\n// test store, not a real shop being onboarded.\ndelete_transient( '_wc_activation_redirect' );\nupdate_option( 'woocommerce_onboarding_profile', array( 'skipped' => true ) );\n\n// Seed a few products with SKUs so catalog sync has something to push.\nif ( class_exists( 'WC_Product_Simple' ) ) {\n\t$products = array(\n\t\tarray( 'name' => 'Calming Cleanser', 'sku' => 'OYS-CLN-001', 'price' => '24.00' ),\n\t\tarray( 'name' => 'Vitamin C Serum', 'sku' => 'OYS-SER-002', 'price' => '48.00' ),\n\t\tarray( 'name' => 'Barrier Repair Moisturizer', 'sku' => 'OYS-MOI-003', 'price' => '32.00' ),\n\t);\n\tforeach ( $products as $p ) {\n\t\tif ( wc_get_product_id_by_sku( $p['sku'] ) ) {\n\t\t\tcontinue;\n\t\t}\n\t\t$product = new WC_Product_Simple();\n\t\t$product->set_name( $p['name'] );\n\t\t$product->set_sku( $p['sku'] );\n\t\t$product->set_regular_price( $p['price'] );\n\t\t$product->set_status( 'publish' );\n\t\t$product->set_catalog_visibility( 'visible' );\n\t\t$product->save();\n\t}\n}\n"
    }
  ]
}
